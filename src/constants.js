const METADATA = Symbol(Date.now());
const AppState = require('./appState.js');
const CONTEXT_DEFAULT = Symbol(Date.now());
const PRIMARY = Symbol(Date.now());


module.exports = {  
    AppState, METADATA, CONTEXT_DEFAULT, PRIMARY
}